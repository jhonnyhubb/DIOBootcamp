let n = 34;
let o = 5;

text = "life is not a problem to be solved"

if( n >= 1 && n <= 34) {
    const textIncomplete = text.substr( 0, n );
    const upper = textIncomplete.toUpperCase();

    console.log( upper );
}

if( o >= 1 && o <= 34) {
    const textIncomplete = text.substr( 0, o );
    const upper = textIncomplete.toUpperCase();

    console.log( upper );
}